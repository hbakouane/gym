<div class="gradient-bg-6 py-10">
    <p class="h3 text-center text-light">{{ __('external.Haven\'t started yet? We\'re offering a free trial!') }}</p>
    <div class="text-center my-5">
        <a href="{{ route('project.create') }}" class="btn btn-light shadow">{{ __('external.trial') }}</a>
        <a href="{{ route('project.create') }}" class="btn btn-success shadow">{{ __('project.Create a Project') }}</a>
    </div>
    <p class="small my-5 text-light text-center">{{ __('external.credit-card') }}</p>
</div>
